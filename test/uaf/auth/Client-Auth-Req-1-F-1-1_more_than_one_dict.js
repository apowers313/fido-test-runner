// not sure if these tests should all be separate files, or if some of them should be in the same file...
// ...lots of describe()s end up being a little ugly in the reporter

describe ("UAF Auth Req 1-F-1-1", function() {
    it ("more than one dict", function() {
        
        // TODO: send message with more than one dict
        // var msg = _.deepClone(helper.default_message());
        // msg.someDict = {};
        // server.sendClient(msg);

        // TODO: verify that the test fails
        // var resp = server.getResponse();
        // server.verifyResponse(resp);
    });
});